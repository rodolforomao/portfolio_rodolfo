Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve arquivos e diretórios existentes normalmente
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# React Router — redireciona rotas SPA para index.html
RewriteRule ^ index.html [L]
