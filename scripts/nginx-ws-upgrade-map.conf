# Map para proxy WebSocket — incluir no bloco http {} do nginx.
# Instalado em /etc/nginx/conf.d/ws-upgrade-map.conf pelo deploy-vault-services.sh

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
